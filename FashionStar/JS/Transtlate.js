export function Translate(x, t){
  let myNode=x;
  let myTime=t;
  this.start=function(){
   let List=document.querySelectorAll(myNode);
   let images=[]
   for(let i=0;i<List.length;i++){
      images.push(List[i]);
   }  
   images.forEach((el)=>{
     
   })
  }
}